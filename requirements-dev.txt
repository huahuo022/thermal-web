# 本地调试用的可选依赖。
#
# 运行 thermal-web 本身什么都不需要装（纯 Python 标准库，服务器上也是）：
#   python server.py --port 8099 --data ./data
#
# 只有下面这些是"本地调试可能想要"的：
#   .\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
#
# Pillow: 让 POST /api/print 的 kind=image-file（服务端抖动）可用。
#         走浏览器端抖动的 kind=bitmap 不需要它 —— 线上就是这么跑的。
Pillow>=10
